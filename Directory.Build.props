<Project>

  <PropertyGroup Label="Metadata">
    <Authors>basverweij</Authors>
    <Company>app-bld</Company>
    <Copyright>Copyright (c) applicationbuildersalmanac.com</Copyright>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <RepositoryUrl>https://github.com/app-bld/app-bld-mapping</RepositoryUrl>
  </PropertyGroup>

  <PropertyGroup Label="Version">
    <VersionPrefix>0.1.0</VersionPrefix>
    <VersionSuffix>$(VersionSuffix)</VersionSuffix>
  </PropertyGroup>

  <PropertyGroup>
    <LangVersion>8.0</LangVersion>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>